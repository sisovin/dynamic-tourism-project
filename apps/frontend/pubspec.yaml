name: dynamic_tourism_project
description: A Flutter web app for the Dynamic Tourism Project
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_tailwind: ^0.1.0
  flutter_rating_bar: ^4.0.0
  cached_network_image: ^3.0.0
  provider: ^5.0.0
  flutter_bloc: ^7.0.0
  freezed_annotation: ^0.14.0
  dio: ^4.0.0
  sentry_flutter: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  freezed: ^0.14.0

flutter:
  uses-material-design: true
